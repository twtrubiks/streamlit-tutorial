# ── 環境變數範本 ──────────────────────────────────────────────
# 使用方式: 複製這支檔案成 .env, 再依需求修改裡面的值。
#
#   cp .env.example .env
#
# docker compose 會自動讀取同目錄的 .env, 把值帶進 docker-compose.yml 的 ${DOMAIN}。
# 真正的 .env 已被 .gitignore 排除 (不會進版控); 這支 .env.example 才是要進版控的範本。

# Caddy 要服務的網域 (用 --profile https 啟動 HTTPS 時才會用到)。
#   - 正式環境: 填你的真實網域, 且該網域 DNS A 紀錄已指到本機
#   - 本機測試: 留 localhost (Caddy 會用自簽憑證)
DOMAIN=localhost
