# 反向代理到 Streamlit 容器, 由 Caddy 自動處理 HTTPS。
# {$DOMAIN} 由 docker-compose.https.yml 的環境變數帶入:
#   - DOMAIN=真實網域  -> Caddy 自動跟 Let's Encrypt 申請正式憑證並續期
#   - 未設定 (預設 localhost) -> Caddy 用內建 CA 簽自簽憑證, 供本機測試
#
# reverse_proxy 預設就會正確轉發 WebSocket (Streamlit 需要), 不用額外設定。
{$DOMAIN} {
	reverse_proxy app:8501
}
